# === Core runtime ===
numpy<2
pandas>=2.2
pydantic>=2.7
pyyaml>=6.0
duckdb>=1.0

# === LLM & AI Framework ===
openai>=1.33.0          # OpenAI API client
langchain>=0.1.0        # LangChain framework for LLM interactions
langchain-openai>=0.1.0 # LangChain OpenAI integration
httpx>=0.25.0           # HTTP client for API calls

# === UI & Visualization ===
streamlit>=1.28.0
plotly>=5.22
matplotlib>=3.8,<3.9
Pillow>=10.0

# === Time-series & Statistics ===
statsmodels>=0.14
pmdarima>=2.0
scikit-learn>=1.4

# === Utilities ===
python-dotenv>=1.0       # load env vars from .env locally
tenacity>=8.2            # retries for API calls
watchdog>=3.0            # file system monitoring for Streamlit

# === Type Hints ===
types-simpleNamespace    # for SimpleNamespace type hints

# === Optional ML frameworks ===
# prophet>=1.1 ; sys_platform != "win32"  # optional; can be finicky to build
# tensorflow~=2.16                        # optional; for advanced ML models